<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="japan-pictograms.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BmbxuPwQa2lc/FVzBcNJ7UAyJxM6wuqIj61tLrc4wSX0szH/Ev+nYRRuWlolflfl" crossorigin="anonymous">
  <title>Document</title>
</head>
<div class="container">
  <h1 class="text-center m-5">Experience Japan Pictograms - icons repo</h1>
  <h2>Summary</h2>
  <div class="list-group w-25 mb-5">
    <a href="#setup" class="list-group-item list-group-item-action">Setup</a>
    <a href="#usage" class="list-group-item list-group-item-action">Usage</a>
    <a href="#size" class="list-group-item list-group-item-action"> Sizes</a>
    <a href="#pictos" class="list-group-item list-group-item-action"> Pictograms</a>
  </div>
  <h2 id="setup">Setup</h2>
  <p>Add this <code>link</code> to your HTML file <code>&lt;head&gt;</code>:</p>
    <pre class="border rounded p-3"><code>&ltlink rel="stylesheet" href="japan-pictograms.css"&gt;</code></pre>
  <h2 id="usage">Usage</h2>
  <p>Add icons following this pattern, refer to the below table for the SIZE and NAME:</p>
    <pre class="border rounded p-3"><code>&ltspan class="jpic-SIZE jpic-NAME"&gt&lt/span&gt;</code></pre>

  <h3 id="size" >Sizes</h3>
  <table class="table">
    <thead>
      <tr>
        <th scope="col">SIZE</th>
        <th scope="col">Sample code</th>
        <th scope="col">Result</th>
      </tr>
    </thead>
    <tbody>
      <% ['xs', 'sm', 'md', 'lg', 'xl'].each do |size| -%>
      <tr>
        <td><%= size %></td>
        <td><code>&lt;span class="jpic-<%= size -%> jpic-<%= pictos[0][:name] -%>"&gt;</code></span></td>
        <td><span class="jpic-<%= size -%> jpic-<%= pictos[0][:name] -%>"></span></td>
      </tr>
      <% end -%>
    </tbody>
  </table>

  <h3 id="pictos" >Pictograms</h3>
  <table class="table">
    <thead>
      <tr>
        <th scope="col">NAME</th>
        <th scope="col">Sample code</th>
        <th scope="col">Result</th>
      </tr>
    </thead>
    <tbody>
      <% pictos_hash[:pictos].each do |picto| -%>
      <tr>
        <td><%= picto[:name] %></td>
        <td><code>&lt;span class="jpic-md jpic-<%= picto[:name] -%>"&gt;</code></span></td>
        <td><span class="jpic-md jpic-<%= picto[:name] -%>"></span></td>
      </tr>
      <% end -%>
    </tbody>
  </table>
</body>
</html>
